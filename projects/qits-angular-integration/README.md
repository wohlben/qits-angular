# @qits/angular

Integration library for Angular apps managed by [qits](https://github.com/wohlben/qits).
Walking skeleton — `provideQitsIntegration()` / `initQitsIntegration()` are currently no-ops.

See the repository root `README.md` for install, usage, and the git-only distribution contract.
